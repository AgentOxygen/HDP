[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "HDP_python"
version = "1.1.0"
description = "Heatwave Diagnostics Package (HDP) for computing heatwave metrics over gridded timeseries data."
authors = [
  { name = "Cameron Cummins", email = "cameron.cummins@utexas.edu" },
]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
requires-python = ">=3.12.3"
dependencies = [
  "numpy",
  "xarray >= 2025.1.1",
  "cartopy",
  "numba >= 0.60.0",
  "nc_time_axis",
  "dask[complete]",
  "zarr",
  "netCDF4",
  "tqdm",
  "ipywidgets",
  "nbformat"
]

keywords = ["HDP", "heat", "heatwave", "heatwave diagnostics package", "climate", "climate model", "timeseries", "heatwave metrics", "diagnostics"]

[project.urls]
Homepage = "https://github.com/AgentOxygen/HDP"
Documentation = "https://hdp.readthedocs.io/"
Issues = "https://github.com/AgentOxygen/HDP"

[tool.pytest.ini_options]
# The container runs pytest as an unprivileged user without a writable cache dir,
# so disable the cache to avoid PytestCacheWarning permission errors.
addopts = "-p no:cacheprovider"
# ax.coastlines() downloads Natural Earth data on first use; this is expected.
filterwarnings = [
    "ignore::cartopy.io.DownloadWarning",
]

[tool.setuptools]
packages = ["hdp"]

[tool.setuptools.package-data]
hdp = [
    "graphics/hdp.mplstyle"
]